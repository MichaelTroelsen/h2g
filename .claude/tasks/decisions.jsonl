{"id": "regrid-is-not-in-fidelity-toggles-so-the-search-never-walks-it", "kind": "decision", "asked": "(not asked -- answered by research before any question was put)", "answer": "(none needed)", "resolved": true, "head": "c490b72", "new_mode": null, "notes": "ALREADY SATISFIED. python/presets.py:190-244, the `regrid` entry in FIXED, carries the decision in full, recorded at v0.5.411: regrid stays OUT of FIDELITY_TOGGLES. The reason is NOT a measurement gap -- fidelity.drift takes the same two traces play() already holds and drift_per_1000 IS a registered Dimension. It is that fidelity_better's `keeps_notes` requires cand[2] >= ref[2] on OUR attack count and gates every acceptance term, so all four eligible files are refused before any term is consulted -- Arcade_Classics 375->372 (original 372), Sigma_Seven 417->414, Wiz 446->437, Rikky 201->196 (original 197), every one a strict improvement the guard reads as a loss. The file's own words: 'the exclusion list should NOT keep growing, and the term to add is not drift but the original's attack count in keeps_notes'. CLAUDE.md:1386 says the same. Nothing to decide, nothing to run -- the next /whattask should move this to `closed` with this evidence as the reason. NOT flipped to a runnable mode."}
{"id": "the-two-incomparable-known-bad-pairs-need-retiring-or-replacing-and-that-is-a-coverage-decision", "kind": "decision", "asked": "Las_Vegas and Samantha_Fox can never be `seen` by the sound calibration -- their v0.5.401 fix is a multiplier change, so the good build repacks at a different -S and loud_ratio lands at 0.074x/0.063x. While they remain, all(seen) can never hold and the doc reads FAIL forever. This gates three tasks. How should the calibration handle them?", "answer": "Keep + report them", "resolved": true, "head": "c490b72", "new_mode": "main", "notes": "Unblocks three tasks: this one, `the-inherited-approval-state-is-unreachable-until-the-sound-calibration-passes`, and `the-render-check-should-be-re-run-once-the-calibration-passes-because-inherited-has-never-been-observed`. The deletion route was refused, consistent with its own refusal at 7bcd3fe."}
{"id": "a-corpus-fidelity-refresh-is-owed-and-would-move-six-old-songs-hard-restart-settings", "kind": "decision", "asked": "A full --fidelity preset search was run at 60 s and diffed but never adopted (C:/t/hr1_candidate.json). Since it was measured the window moved 60 -> 180 s, and the one experiment comparing them found they choose differently on 25 fields across 17 of 89 songs. Re-run at 180 s, adopt as measured, or drop?", "answer": "Re-run at 180 s", "resolved": true, "head": "c490b72", "new_mode": "main", "notes": "The 60 s candidate is NOT adopted. Cost measured at v0.5.459: ~53 min over six parallel shards, ~75 min serial. A sharded run needs --carry-from or presets.py refuses rather than silently dropping the per-song decisions it cannot re-derive."}
{"id": "powerplay-regrid-refusal-rests-on-a-naming-artefact-and-should-be-re-decided", "kind": "decision", "asked": "Powerplay refuses --regrid on a 2.88pp melody drop that is 94% a naming artefact. The analogous case (One_on_One) was refused on the same evidence shape and REVERSED when the window moved 60 -> 180 s. Adopt now, re-measure at 180 s first, or keep the refusal?", "answer": "Re-measure at 180 s first", "resolved": true, "head": "c490b72", "new_mode": "main", "notes": "The answer carries its own decision rule, so the runner does not re-derive it: if the 2.88pp survives at 180 s keep the refusal, if it collapses or reverses as One_on_One's did, adopt. One file, one A/B -- cheap next to the corpus refresh."}
{"id": "the-whattask-generator-lives-outside-the-repo-so-no-plan-touches-can-ever-grant-it", "kind": "decision", "asked": "Three open tasks fix the /whattask generator, which lives in the mit-setup plugin OUTSIDE this repo -- and that directory is a plugin CACHE, not a git repository, so edits there are unversioned and overwritable by a plugin update. How should this class be handled?", "answer": "Separate session on plugin source", "resolved": true, "head": "c490b72", "new_mode": null, "notes": "The decision IS this task's deliverable, so there is nothing left to RUN in this repo -- new_mode null, and the next /whattask should move it to `closed` with this answer as the reason. IT ALSO SETTLES THREE OTHER TASKS, which should be closed as out of scope for this repo rather than left to be attempted again: `whattask-should-check-a-touches-path-exists-before-granting-it` (already `partial` twice for exactly this reason), `creates-should-be-a-third-touches-mode-beside-r-and-rw-because-intent-cannot-be-inferred`, and `the-generators-mechanical-test-file-rule-manufactures-a-second-error-from-a-wrong-source-grant`. Also worth carrying: `runtask-holder-records-carry-a-shell-pid-that-is-dead-before-the-task-is` and `locking-md-fix-is-uncommitted-in-the-plugin-repo` are the same class -- plugin work, not repo work. RESEARCH FINDING behind the answer: `git status` in the plugin cache returns 'fatal: not a git repository', so option (b) would have edited a directory a plugin update replaces."}
{"id": "twenty-four-worktrees-hold-uncommitted-work-from-past-sessions", "kind": "decision", "asked": "(not put -- the task's own figure was found stale during research, so the question would have been asked against a wrong picture)", "answer": "(not asked this sitting)", "resolved": false, "head": "c490b72", "new_mode": null, "notes": "STILL BLOCKED, but the premise is corrected: `git worktree list` returns 45 lines -- the main checkout plus **44 worktrees**, not the 24 the title and blocked_on claim. The count has nearly doubled since it was written, so this is growing rather than waiting. A per-worktree decision over 44 is no longer a single sitting's question; it wants a triage pass that groups them (which hold a diff at all, which are branches already merged) BEFORE a human is asked anything. Sharpened rather than asked."}
{"id": "the-six-interleaved-classic-files-have-never-been-listened-to", "kind": "decision", "asked": "(not put -- research showed the first step is not the human's)", "answer": "(not asked this sitting)", "resolved": false, "head": "c490b72", "new_mode": null, "notes": "STILL BLOCKED on a listening verdict, but MIS-SCOPED as it stands. Confirmed by listing build/listen: 0 of the six files (Radio_ACE, Lion_Heart, Lakers_vs_Celtics, Pacific_Coast, Go_Go_Dash, Sun_Never_Shines) appear there. Staging them is `listen.py`, which is [main] AGENT work, not the human's -- so asking for a verdict now would be asking for something that cannot be given. The human step begins only once the material exists. Sharpened: stage first, then ask."}
